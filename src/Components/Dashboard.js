// import React, { useRef } from "react";
// import { gsap } from "gsap";
// import { useGSAP } from "@gsap/react";
// import { SplitText } from "gsap/SplitText";
import "./App.css";

// gsap.registerPlugin(SplitText);

function Dashboard() {
 
  return (
    <div className="relative w-full h-screen overflow-hidden bg-dark text-white">
      <section className="p-5">
        <p className="p-5 display-1">This project is a series of simple GSAP lessons that teach how to animate elements using the GSAP methods, to focus on learning animation concepts step-by-step.</p>
      </section>
    </div>
  );
}

export default Dashboard;
